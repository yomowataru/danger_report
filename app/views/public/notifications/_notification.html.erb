<!--<div class="notification-view">-->
  <!--< %= notification_form(notification) %><span class="moderate-font">< %= " (#{time_ago_in_words(notification.created_at)}前)" %></span>-->
  <!--<br>-->
  <!--< % if !@comment.nil? %>-->
    <!--<p class="notification-comment">< %= @comment %></p>-->
  <!--< % end %>-->
<!--</div>-->

<% visiter = notification.visiter %>
<% report = notification.report %>

<ul>
    <li><strong><%=visiter.name %></strong>さんが
    <strong><%= link_to report_path(notification.report_id) do %><%= report.introduction.truncate(20)%><% end %></strong>にコメントしました。
    <%= " (#{time_ago_in_words(notification.created_at)} 前)" %></li>

</ul>

